Run the below commands in the myapp/static folder

1. sudo -E npm install
2.compile:
./node_modules/.bin/webpack --config webpack-production.config.js
3. watch mode:
./node_modules/.bin/webpack --config webpack-production.config.js --watch
